function magnus() {
    let r = 1;
    let rho = 1.225;

    let omega_magnitude = 1;

    let omega = new Victor();
    let v = new Victor(1, 1);

    let f1 = (Math.pow(Math.PI, 2) * Math.pow(r, 3) * rho) * omega


}